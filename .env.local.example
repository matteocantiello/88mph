# Spotify API Credentials
# Get yours at https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_client_id_here
SPOTIFY_CLIENT_SECRET=your_client_secret_here

# Spotify Playlist Creation (pre-created playlists on the 88mph account)
# Get a refresh token by running: node scripts/spotify-get-token.mjs
# Requires Spotify Premium (Web API requirement since Feb 2026)
SPOTIFY_REFRESH_TOKEN=your_refresh_token_here

# Save to Spotify — legacy per-user playlist creation (requires Spotify dev mode)
# Set to "true" to show the Save to Spotify button (fallback when no pre-created playlist exists)
NEXT_PUBLIC_ENABLE_SPOTIFY_SAVE=false
# Generate a secure random value with: openssl rand -hex 32
SPOTIFY_COOKIE_SECRET=your_64_char_hex_string_here
# Your app's public URL (no trailing slash)
# For local dev use http://127.0.0.1:3000 (Spotify does NOT allow "localhost")
# For production use your https:// URL
NEXT_PUBLIC_BASE_URL=http://127.0.0.1:3000
